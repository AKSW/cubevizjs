import {assert} from 'chai';
import {showPopover} from '../../src/actions';

describe('Action tests', function() {

    it('should return a changeImportSettings action', function() {
        console.log(showPopover(true));
    });
});
